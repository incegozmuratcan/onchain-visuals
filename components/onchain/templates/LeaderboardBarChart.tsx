"use client";
import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { MetricCard } from "../ChartShell";
export function LeaderboardBarChart({ data }: { data:any }) { const bars=(data.series?.bars||[]).slice(0,12); return <><div className="grid grid-cols-3 gap-4">{(data.headlineMetrics||[]).slice(0,3).map((m:any)=><MetricCard key={m.label} label={m.label} value={m.formattedValue}/>)}</div><div className="mt-8 h-[520px] rounded-3xl border border-zinc-200 bg-white p-6"><ResponsiveContainer width="100%" height="100%"><BarChart data={bars} layout="vertical" margin={{left:40,right:40,top:10,bottom:10}}><XAxis type="number" hide/><YAxis type="category" dataKey="name" width={180} tick={{fontSize:16,fill:'#18181b'}}/><Tooltip formatter={(v:any)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:'compact'}).format(Number(v))}/><Bar dataKey="value" fill="#111827" radius={[0,12,12,0]}/></BarChart></ResponsiveContainer></div></>; }
