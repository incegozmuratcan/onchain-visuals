"use client";
import { Bar, BarChart, LabelList, ResponsiveContainer, XAxis, YAxis } from "recharts";
import { MetricCard } from "../ChartShell";
const usd = (v:any)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:'compact',maximumFractionDigits:2}).format(Number(v)||0);
export function LeaderboardBarChart({ data }: { data:any }) { const bars=(data.series?.bars||[]).slice(0,12); return <><div className="grid grid-cols-3 gap-4">{(data.headlineMetrics||[]).slice(0,3).map((m:any)=><MetricCard key={m.label} label={m.label} value={m.formattedValue}/>)}</div><div className="mt-7 h-[500px] rounded-3xl border border-zinc-200 bg-white p-6"><ResponsiveContainer width="100%" height="100%"><BarChart data={bars} layout="vertical" margin={{left:24,right:96,top:10,bottom:10}} barCategoryGap={10}><XAxis type="number" hide/><YAxis type="category" dataKey="name" width={190} tick={{fontSize:15,fill:'#18181b'}} tickLine={false} axisLine={false}/><Bar dataKey="value" fill="#111827" radius={[0,12,12,0]} isAnimationActive={false} activeBar={false as any}><LabelList dataKey="value" position="right" formatter={usd as any} style={{ fill:'#3f3f46', fontSize:13, fontWeight:600 }}/></Bar></BarChart></ResponsiveContainer></div></>; }
