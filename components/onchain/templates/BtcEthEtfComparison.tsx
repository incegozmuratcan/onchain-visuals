"use client";
import { Bar, BarChart, LabelList, ResponsiveContainer, XAxis, YAxis } from "recharts";
import { MetricCard } from "../ChartShell";
const usd = (v:any)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:'compact',maximumFractionDigits:2}).format(Number(v)||0);
export function BtcEthEtfComparison({ data }: { data:any }) { return <><div className="grid grid-cols-3 gap-4">{(data.headlineMetrics||[]).slice(0,3).map((m:any)=><MetricCard key={m.label} label={m.label} value={m.formattedValue}/>)}</div><div className="mt-7 h-[500px] rounded-3xl bg-zinc-950 p-8 text-white"><h3 className="text-sm uppercase tracking-[0.2em] text-zinc-400">Rolling ETF flow battle</h3><ResponsiveContainer width="100%" height="90%"><BarChart data={data.series?.bars || []} margin={{left:20,right:80,top:24,bottom:20}}><XAxis dataKey="name" tick={{fill:'#e4e4e7'}} tickLine={false}/><YAxis tickFormatter={usd as any} tick={{fill:'#e4e4e7'}} tickLine={false}/><Bar dataKey="value" fill="#f97316" radius={[12,12,0,0]} isAnimationActive={false} activeBar={false as any}><LabelList dataKey="value" position="top" formatter={usd as any} style={{ fill:'#f4f4f5', fontSize:13, fontWeight:600 }}/></Bar></BarChart></ResponsiveContainer></div></>; }
