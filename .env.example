# Admin/manual publishing
ONCHAIN_PUBLISH_SECRET=

# X publishing
# Local safety default: set to true to validate/render without calling X APIs.
X_DRY_RUN=true
# User-context token used server-side for media upload and post creation.
X_USER_ACCESS_TOKEN=
# Optional legacy/fallback name supported by the publish route.
X_ACCESS_TOKEN=
